#include<stdio.h>
int main()
{
char name, password, password_again;
int i,j;
char symbols [%,!,*,^,&];
pirntf("Enter name");
scanf(" &c ",&name);
printf("Enter password");
scanf(" %c " , password);
for (i=0; password=8; i++){
 if (password==8 && password==symbols[i])
printf("Enter password again");
scanf(" &c ",password_again);
if (password_again=password)
printf("You know what? You can be a great programmer. Keep Coding!");
}

else {
printf("invalid password");
}
return 0;
}